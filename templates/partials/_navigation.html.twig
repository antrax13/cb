{% set current_path = app.request.get('_route') %}
<nav class="navbar navbar-expand-md navbar-light bg-light fixed-top" style="border-bottom: 1px solid #ddd;">
    <a class="navbar-brand" href="#">
        <img src="{{ asset('images/logo.svg') }}" height="60">
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarsExampleDefault">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item {{ 'customer' in current_path ? 'active' }}">
                <a class="nav-link" href="{{ path('customers') }}"><i class="fas fa-users"></i> Customers <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item {{ 'quote' in current_path ? 'active' }}">
                <a class="nav-link" href="{{ path('quotes') }}"><i class="fas fa-file-alt"></i> Quotes</a>
            </li>
            <li class="nav-item dropdown {{ 'admin' in current_path ? 'active' }}">
                <a class="nav-link dropdown-toggle" href="#" id="dropdown01" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><i class="fas fa-cogs"></i> Admin</a>
                <div class="dropdown-menu" aria-labelledby="dropdown01">
                    <a class="dropdown-item" href="{{ path('admin_shipping') }}">Shipping Text</a>
                    <a class="dropdown-item" href="{{ path('admin_payment') }}">Payment Text</a>
                </div>
            </li>
        </ul>
        <div class="form-inline my-2 my-lg-0">
            <a href="https://www.facebook.com/CocktailBrandalism/" target="_blank" class="btn btn-outline-primary my-2 my-sm-0"><i class="fab fa-facebook-square"></i> Cocktail Brandalism</a>
        </div>
    </div>
</nav>