{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-body">
            <div class="row">
                <div class="col-md-6">
                    <img class="img-fluid" src="{{ asset('images/products/'~product.image) }}">
                </div>
                <div class="col-md-6">
                    <div class="alert alert-info">
                        <h1 class="display-4">{{ product.name }}</h1>
                        <p class="lead">
                            Introduction to be coded
                        </p>
                        <p>
                            {{ product.description }}
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

